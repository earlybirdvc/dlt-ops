---
description: The complete dlt-ops CLI reference — every command and option, generated from the CLI itself so this page and dlt-ops <command> --help render the same text.
---

# CLI reference

Every `dlt-ops` command and option, generated from the CLI itself — this page and `dlt-ops <command> --help` render the same text. The project root comes from the top-level `--root` option, or is found by walking up from the working directory to the nearest `.dlt/config.toml`. For task-shaped walkthroughs, start from the [guides](../guides/add-a-source.md); for the config keys these commands read, see the [config reference](../configuration/reference.md).

::: mkdocs-click
    :module: dlt_ops.cli.cli
    :command: cli
    :prog_name: dlt-ops
    :depth: 1
